services:
  web:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - PORT=8080
      - TOKKA_TTL_MS=90000
      - CONTACT_TO=laura@badi108realestate.com
    restart: unless-stopped
